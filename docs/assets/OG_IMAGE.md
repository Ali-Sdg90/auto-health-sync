# Open Graph Image

The social preview image is `docs/assets/og-image.jpg`.

- Keep replacements at 1200 × 630 pixels in JPEG format.
- Preserve the app name, icon, and personal health data pipeline message.
- Keep important content away from the outer 80 pixels and never include personal health data.
